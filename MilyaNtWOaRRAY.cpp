#include<bits/stdc++.h>
using namespace std;

void findDis(){

}

int main(){
    int t;
    cin>>t;
    while(t--){
        int n;
        cin>>n;
        int arr[n];
        int brr[n];
        for(int i=0;i<n;i++){
            cin>>arr[i];
        }
        for(int i=0;i<n;i++){
            cin>>brr[i];
        }
         int size1 = sizeof(arr) / sizeof(arr[0]);
         int size2 = sizeof(brr) / sizeof(brr[0]);

        vector<int> vec1(arr, arr + size1);
        vector<int> vec2(brr, brr + size2);
       

          for(int i=0;i<n;i++){
           
        }
        


        

    }
}